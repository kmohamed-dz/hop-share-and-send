# Capacitor mobile assets

These are placeholder assets for mobile icon/splash generation:

- `icon-placeholder.svg`
- `splash-placeholder.svg`

Generate native assets after replacing with your final branding:

```bash
npx @capacitor/assets generate
```

Or provide PNG assets (`icon.png` and `splash.png`) before generation.
